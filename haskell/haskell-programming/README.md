# Haskell-Programming #

This was a pretty cool little project to actually do something with Haskell.  The video didn't match the final code though.  He added some functions that called functions that weren't in this code, so I was never able to add the random characters to the grid to complete the game.